# Upstream Ethereum RPC (use a fast, reliable provider)
UPSTREAM_RPC=https://eth.rpc.bloxroute.com

# Flashbots Relay endpoint (for bundle execution)
FLASHBOTS_RELAY=https://relay.flashbots.net

# Port to run the gateway on
PORT=8545

# MEV fee percentage (0.10 = 10%)
FEE_PERCENTAGE=0.10
